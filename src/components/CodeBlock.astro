---
import { Code } from "astro-expressive-code/components";

const { content, language, mark, ins, del, meta } = Astro.props;
---

<Code code={content} lang={language} mark={mark} ins={ins} del={del} meta={meta}/>
